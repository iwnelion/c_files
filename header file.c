#include <stdio.h>
#include "myfile.h"
//or
#include "folder/myfile.h"
//header file: it ends with .c instead of .header
//instead of the implementations of your functions and the other parts of a program, 
//it holds the declarations.

int main()
{
//program that uses another program (myfile.c)
    return 0;
}
//this file doesn't do anything. it's just the basic syntax for header files.
